{
  "name": "dolist",
  "version": "1.0.0",
  "description": "A simple app for organizing to-do lists",
  "main": "index.js",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "start": "node build/server",
    "dev-start": "nodemon build/server",
    "dev-build": "rm -r build && tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Pool",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "nodemon": "^2.0.15",
    "typescript": "^4.0.3"
  }
}
